{
  "name": "@workspace/testing-three-js-mach",
  "version": "1.0.0",
  "scripts": {
    "build": "$(node ./get-bin.mjs) build ./src/index.js --no-optimize",
    "build:optimize": "$(node ./get-bin.mjs) build ./src/index.js"
  },
  "devDependencies": {
    "@alshdavid/mach": "workspace:*",
    "enhanced-resolve": "^5.16.1"
  }
}

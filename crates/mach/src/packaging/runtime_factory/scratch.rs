  // // let code = "await require_async([], \"module_id\").then(imprt => {})";
  // // let result = parse_module(&PathBuf::from(""), code, Arc::new(SourceMap::default())).unwrap();
  // // dbg!(result.module);
  // // return ;


  // let rf = runtime_factory::RuntimeFactory::new(Arc::new(SourceMap::default()));

  // let stmt = rf.define_reexport_namespace(&["test"], "module", Some("key".to_string()));

  // let rendered = render_stmts(&vec![stmt], Arc::new(SourceMap::default()));

  // println!("{}", rendered);
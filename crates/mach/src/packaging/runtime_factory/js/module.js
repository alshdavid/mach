// ESM
mach_init["MODULE_ID"] = async (mach_require, define_export, define_reexport) => {
  // module code
};

// CJS
mach_init["MODULE_ID"] = (mach_require, define_export, define_reexport, module, exports) => {
  // module code
};

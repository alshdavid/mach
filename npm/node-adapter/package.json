{
  "name": "@workspace/adapter",
  "private": true,
  "version": "0.0.0-workspace",
  "main": "lib/index.js",
  "types": "types/index.d.js",
  "scripts": {
    "build": "rm -rf lib types && npx tsc && npx tsc --outDir types --declaration true --emitDeclarationOnly true"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}

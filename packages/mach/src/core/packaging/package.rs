use crate::types::Compilation;

pub fn package(Compilation { .. }: &mut Compilation) -> anyhow::Result<()> {
  // let mut queue = vec![];

  // while let Some(bundle) = queue.pop() {}

  return Ok(());
}
